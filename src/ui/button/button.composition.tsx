import React, { ReactElement } from "react";
import { Button } from "./button";

// sets the Component preview in gallery view
const BasicButton = (): ReactElement => {
  return <Button>click me</Button>;
};

export default BasicButton;
